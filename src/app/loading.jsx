import LoadingSection from '@/Companent/LoadingSection/LoadingSection';
import React from 'react';

const loading = () => {
    return (
        <div>
         <LoadingSection></LoadingSection>
        </div>
    );
};

export default loading;